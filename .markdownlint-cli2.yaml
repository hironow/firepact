gitignore: true
# docs/history/{design,handoff}.md are original seed documents (historical
# context) kept verbatim; everything else is linted.
ignores:
  - docs/history/design.md
  - docs/history/handoff.md
  - node_modules/**
  - .venv/**
  - target/**
  - dist/**
  - output/**
  - private/**
