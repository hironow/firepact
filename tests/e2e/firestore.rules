rules_version = '2';

// Emulator-only rules for the e2e run -- NEVER use in production.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
